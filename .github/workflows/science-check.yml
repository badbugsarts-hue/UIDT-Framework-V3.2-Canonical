name: UIDT Scientific Integrity Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  mathematical-closure:
    name: Verify Mass Gap & Gamma
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
        cache: 'pip'
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: Run Canonical Verification
      # Dieser Schritt schlägt fehl, wenn das Skript einen Error wirft
      run: |
        echo "🚀 Running High-Precision Verification..."
        python src/verification/UIDT-3.5-Verification.py
      
    - name: Run Cosmology Unit Tests
      run: |
        echo "🌌 Verifying Hubble Tension Resolution..."
        python -m pytest tests/
